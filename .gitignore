node_modules/
dist/
*.log

# The context graph's local LLM-call cache — regenerated, never committed.
# The graph itself (.context/*.md + manifest.json) IS committed.
.context/.cache/

# Benchmark run artifacts (regenerated per run)
bench/results/

# Env
.env
.env.local

# OS
.DS_Store
